# Browser-safe configuration only. Never place keys, passwords, tokens, or
# database connection strings in a VITE_ variable: Vite exposes them to users.
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=VidyaLink
